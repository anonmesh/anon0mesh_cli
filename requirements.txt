# anon0mesh RPC Bridge — Python MVP
# ────────────────────────────────────────────────────────────────────────────────

# Core mesh transport (Reticulum Network Stack)
rns>=0.7.0

# Lightweight Extensible Message Format (beacon discovery)
lxmf

# HTTP client for forwarding RPC calls (beacon-side only)
requests>=2.31.0

# Terminal QR code display (wallet address in menu)
qrcode>=7.4.2

# Optional: offline transaction signing
# Uncomment if you want to use --sign-offline on the client
# solders>=0.20.0
